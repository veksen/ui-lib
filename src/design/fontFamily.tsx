const FONT_RUBIK = "Rubik, sans-serif";
const FONT_ARCHIVO_NARROW = "'Archivo Narrow', sans-serif";

const fontFamily = {
  emphasis: FONT_RUBIK,
  headline: FONT_ARCHIVO_NARROW,
  body: FONT_RUBIK,
  button: FONT_RUBIK,
  input: FONT_RUBIK
};

export default fontFamily;
